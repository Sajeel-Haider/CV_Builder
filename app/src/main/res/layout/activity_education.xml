<?xml version="1.0" encoding="utf-8"?>
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/educationActivity"
    android:background="@android:color/black"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Institution Name Input -->
        <EditText
            android:id="@+id/etInstitution"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Enter Institution Name"
            android:textColor="@android:color/white"
            android:background="@color/background_color_secondary"
            android:padding="8dp" />

        <!-- Degree/Qualification Input -->
        <EditText
            android:id="@+id/etDegree"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Enter Degree/Qualification"
            android:textColor="@android:color/white"
            android:background="@color/background_color_secondary"
            android:padding="8dp"
            android:layout_marginTop="8dp" />

        <!-- Additional Education Details Input -->
        <EditText
            android:id="@+id/etEducationDetails"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Enter additional details (e.g., duration, major)"
            android:textColor="@android:color/white"
            android:background="@color/background_color_secondary"
            android:minLines="3"
            android:padding="8dp"
            android:layout_marginTop="8dp" />

        <!-- Save Button -->
        <Button
            android:id="@+id/btnSave"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Save"
            android:textColor="@android:color/white"
            android:background="@color/dark_gray"
            android:layout_marginTop="16dp" />

        <!-- Cancel Button -->
        <Button
            android:id="@+id/btnCancel"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Cancel"
            android:textColor="@android:color/white"
            android:background="@color/dark_gray"
            android:layout_marginTop="8dp" />

    </LinearLayout>
</ScrollView>
